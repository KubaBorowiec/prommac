h2
	| Aktualności
div
	div(class="sort")
					button.date(ng-click="sortBy('date')" add-class="activeButton") 
						svg-clock
						| Data
					//-button(ng-click="sortBy('title')")
						| Tytuł
					button.figth(ng-click="myFilter = {cat:'walka'}" add-class="activeButton")
						svg-fist
						| Walka
					button(ng-click="myFilter = {cat:'media'}" add-class="activeButton")
						svg-mircophone
						| Media
					button(ng-click="myFilter = {cat:'zawodnicy'}" add-class="activeButton")
						svg-boxer
						| Zawodnicy
					button(ng-click="myFilter ='' " add-class="activeButton")
						svg-tick
						| Wszystkie
	div.container
		div.row
			div(class="id-{{post.id}} posts col-xs-12 col-md-6 col-lg-4" ng-repeat="post in posts | orderBy:sorting | filter:myFilter | limitTo:itemsPerPage:begin")
					header
						h3 {{post.title}}		        
					article
								img(ng-src="images/posts/{{post.src}}")
					footer(class="grey clearfix")
								div(class="pull-left")
									span 
											| {{post.date | date:"MM/dd/yyyy" }}
								div(class="pull-right cat cat-{{post.cat}}") 
										| Kategoria: 
										span 
											| {{post.cat}}
								div(class="pull-left")
									| Autor: 
									span {{post.author}}
								a(class="pull-right" ng-href="#news/{{post.slug}}")
										span(class="readmore") Czytaj więcej