h2
	| Aktualności
div(class="row")
	div(class="sort")
					button(ng-click="sortBy('date')")
						| Data
					button(ng-click="sortBy('title')")
						| Tytuł
					button(ng-click="myFilter = {cat:'walka'}")
						| Walka
					button(ng-click="myFilter = {cat:'media'}")
						| Media
					button(ng-click="myFilter = {cat:'zawodnicy'}")
						| Zawodnicy
					button(ng-click="myFilter ='' ")
						| Wszystkie
		div(class="pagination-main")
				ul
					li(ng-repeat="a in range(5)")
					|	{{ $index }}
		div(class="id-{{post.id}} posts col-xs-6" ng-repeat="post in posts | orderBy:sorting | filter:myFilter | limitTo:itemsPerPage:begin")
			header
				div(class="clearfix")
				div(class="pull-left")
					| Data: 
				span 
					| {{post.date | date:"MM/dd/yyyy" }}
				h3 {{post.title}}		        
				div(class="cat cat-{{post.cat}}") 
					| Kategoria: 
					span 
					| {{post.cat}}
			article
				img(ng-src="zimages/posts/{{post.src}}")
			footer(class="grey clearfix")
				div(class="pull-left")
					| Autor: 
					span {{post.author}}
					a(class="pull-right" ng-href="#news/{{post.slug}}")
						span(class="readmore") Czytaj więcej