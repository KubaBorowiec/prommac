div.slider(style="height: 305px")
	carousel(interval="myInterval")
		slide(ng-repeat="slide in slides" active="slide.active")
			img(ng-src="{{slide.image}}" style="margin:auto;")
			div(class="carousel-caption")
					p
						a(ng-href="{{slide.href}}")
							| {{slide.text}}